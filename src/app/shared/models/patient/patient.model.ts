export interface Patient {
  address: string; // Ethereum wallet address
  name: string; // Patient's full name
  bio: string; // Basic medical info (e.g., blood type, allergies)
  createdAt: string; // ISO timestamp
}